---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "scaffolding_instance Resource - terraform-provider-scaffolding"
subcategory: ""
description: |-
  Instance resource in the Terraform provider scaffolding.
---

# scaffolding_instance (Resource)

Instance resource in the Terraform provider scaffolding.

## Example Usage

```terraform
resource "scaffolding_instance" "example" {
  name          = "my-instance"
  instance_type = "t2.micro"
  tags = {
    Environment = "production"
    Owner       = "devops"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the instance.
- `instance_type` (String) Type of the instance.

### Optional

- `tags` (Map of String) Tags for the instance.

### Read-Only

- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

```terraform
import {
  to = scaffolding_instance.example
  id = "instance-id"
}
```

For resources created with `for_each`:

```terraform
import {
  for_each = toset(["instance-1", "instance-2", "instance-3"])
  to       = scaffolding_instance.example[each.key]
  id       = each.key
}
```

Or using the legacy command-line syntax:

```shell
terraform import scaffolding_instance.example instance-id